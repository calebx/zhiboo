<%= form_for(@tiezi) do |f| %>
  <% if @tiezi.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tiezi.errors.count, "error") %> prohibited this tiezi from being saved:</h2>

      <ul>
      <% @tiezi.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :link %><br />
    <%= f.text_field :link %>
  </div>
  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_field :url %>
  </div>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
